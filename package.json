{
  "name": "tw-stack",
  "private": true,
  "scripts": {
    "stack": "docker compose up --build",
    "dev": "concurrently --raw -k \"docker compose --ansi=always up --build backend mongo mongo-express\" \"live-server frontend/static --port=8080 --watch=frontend/static --no-browser --proxy=/api:http://localhost:5001/api --proxy=/ws:http://localhost:5001/ws\"",
    "stack:dev": "docker compose up --build frontend backend mongo",
    "clean": "bash -c \"rm -rf data/mongo && mkdir -p data/mongo\"",
    "dev:frontend": "live-server frontend/static --port=8080 --watch=frontend/static --no-browser"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2"
  }
}
