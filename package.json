{
  "name": "tw-stack",
  "private": true,
  "scripts": {
    "stack": "concurrently -k \"docker compose up --build backend mongo mongo-express\" \"live-server frontend/static --port=8080 --watch=frontend/static --no-browser\"",
    "dev": "docker compose up --build",
    "stack:dev": "docker compose up --build frontend backend mongo",
    "clean": "bash -c \"rm -rf data/mongo && mkdir -p data/mongo\"",
    "dev:frontend": "live-server frontend/static --port=8080 --watch=frontend/static --no-browser"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "live-server": "^1.2.2"
  }
}
